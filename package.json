{
  "name": "popcornplay-v2",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "cd functions && npm install && npm install",
    "start": "node server.js",
    "dev-server": "node server.js",
    "emulators": "firebase emulators:start --only hosting,functions",
    "deploy": "firebase deploy --only hosting,functions"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^3.4.0",
    "dotenv": "^16.3.1"
  }
}
